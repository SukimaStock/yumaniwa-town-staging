// Change this file first when starting a new work.
// Keep the id lowercase and URL-safe.
window.SUKIMASTOCK_WORK = Object.freeze({
  id: "new-work",
  title: "NEW WORK",
  logicalWidth: 390,
  logicalHeight: 844,
  frameRate: 60,
});

document.title = window.SUKIMASTOCK_WORK.title;
